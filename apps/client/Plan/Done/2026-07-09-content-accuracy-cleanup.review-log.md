<div dir="rtl">

# سجل مراجعات: تدقيق وتنظيف المحتوى

> يغطي: `apps/client/Plan/2026-07-09-content-accuracy-cleanup.md`، `apps/server/Plan/2026-07-09-content-accuracy-cleanup.md`، `apps/db/Plan/2026-07-09-content-accuracy-cleanup.md`

## جدول الجولات

| جولة | التاريخ | 🔴 | 🟠 | 🟡 (للـ backlog) | الحالة |
|---|---|---|---|---|---|
| 1 | 2026-07-09 | 0 | 1 | 0 | اتصلحت |
| 2 | 2026-07-11 | 0 | 1 | 3 | اتصلحت |
| 3 | 2026-07-11 | 0 | 1 | 0 | اتصلحت |
| 4 | 2026-07-11 | 0 | 1 | 0 | اتصلحت |
| 5 | 2026-07-11 | 0 | 1 | 0 | اتصلحت |
| 6 | 2026-07-11 | 0 | 0 | 0 | نظيفة ✅ |
| 7 | 2026-07-11 | 0 | 0 | 0 | نظيفة ✅ 🔒 |

**حالة الخطة: 🔒 مقفولة** — جولتان نظيفتان متتاليتان (6 و7). جولة 7 كان differential-ها فاضي (جولة 6 ما غيّرتش أي خطة)، فأُعيد التحقق التنفيذي من الادعاءات الحاملة (مواضع التوفّر الـ٥ + نمط الـ regression grep) على الكود الحقيقي — كلها مؤكَّدة. القفل arithmetic من الجدول (آخر جولتين 0🔴/0🟠)، مش انطباع. المتاح دلوقتي: `/my-improve` (اختياري — بند‌ين 🟡 في الـ backlog) ثم `/my-implement`. أي تعديل لاحق على الخطة بيحتاج differential review صريح يكسر القفل.

## جولة 1 — 2026-07-09

**اللي اتغيّر من الجولة اللي فاتت:** أول جولة — مفيش سجل سابق.

**ملاحظة عن العدسات:** الأربع subagents المتوازية (data model، cross-project contract، library/API syntax، security) فشلت كلها بسبب حد جلسة الـ API (session limit). اتعمل الفحص يدويًا بنفس المنهجية عبر قراءة الكود الفعلي مباشرة (Mongoose models، landingController، validators، Hero.vue، about.vue، InfosForm.vue، BaseTextarea في my-base-layer) + فحوصات تنفيذية حقيقية (curl على `/api/v1/projects` و`/api/v1/blogs` على السيرفر المحلي المتصل بـ Atlas، وقياس أطوال كل النصوص/الدرافتات المقترحة برمجيًا).

### النتايج والقرارات

- [x] 🟠 فحص أطوال وصف المقالات (Blog.description) ناقص من خطة السيرفر — بيوثّق بس فحص المشاريع (ورّاق/ترابط) ومفيش ذِكر لنتيجة فحص المقالات، رغم إن Milestone 1 بيطلب فحص `/blogs` كمان. فحص تنفيذي فعلي (curl) كشف إن المقال الوحيد المنشور «تحليل عميق لـ JavaScript Hoisting وتأثيره على الكود» وصفه **206 حرف** — فوق حد الـ160 المقترح. لو الحد اتفعّل من غير تقصيره، أي حفظ لاحق للمقال (حتى لو مش لمس الوصف) هيفشل — لأن `handlerFactory.updateOne` بيستخدم `findByIdAndUpdate(..., {runValidators:true})` وفورمات الـ dashboard بتبعت الحالة كاملة. — **قرار:** اتصلح: أُضيف task في خطة الـ db (Milestone 3 + معايير القبول + Edge cases + الـ Dependencies) لتقصير وصف المقال ده لـ ≤160 قبل تفعيل حد السيرفر، وتحديث خطة السيرفر (Edge cases + Milestone 1 + القرارات المحسومة) لتوثيق نتيجة الفحص كاملة (مشاريع + مقالات). — **دليل التحقق:** `curl http://localhost:1995/api/v1/blogs?fields=title,description&limit=100` (سيرفر محلي متصل بـ Atlas إنتاج) → طول الوصف = 206 حرف عبر `.length` في Node.

### ملاحظة إضافية (مش finding منفصل — دليل تنفيذي مصاحب)

أرقام أطوال المشاريع في الخطة الأصلية كانت قديمة: «ورّاق» موثّق 467 حرف والفحص الحالي بيّن 322، و«ترابط» موثّق 194 والفحص الحالي بيّن 147 (المحتوى اتغيّر من وقت التخطيط). الاستنتاج العملي (ورّاق لسه محتاج تقصير لتحت 200) لسه صحيح. اتحدّثت الأرقام في الخطتين. تأكّدت كمان إن المسودّات الأربعة المقترحة لأوصاف المشاريع كلها فعليًا ضمن 60-200 حرف (188/140/117/111)، وكل أوصاف الـ SEO المقترحة ≤160 حرف فعليًا (37-112) — مفيش مشاكل فيهم.

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **Data model & lifecycle:** `Info.skills` (حقل مضمّن في موديل Info، مختلف عن موديل `Skill` العلوي) بيتستخدم فعليًا في `about.vue` (سكشن المهارات) — اتأكّد إن حذف `Info.stats` في الخطة ما بيلمسوش (الكود في `InfosForm.vue`/`schema.js` بيفرّق بين `addStat`/`addSkill` بوضوح، مفيش تداخل).
- **Cross-project contract:** مفتاح `services`/`skills` العلوي في `/landing` (من `Skill.find()`) منفصل تمامًا عن `info.skills` المضمّن — إزالته مش هتكسر `about.vue`. `useNuxtData('landing')` في خطة الـ Hero.vue متطابق مع الـ key الفعلي في `index.vue`. `resumeUrl` موجود فعليًا في `infoModel.js` (`required:true`) ومتضمّن في الـ `.select()` بتاع `landingController`.
- **Library/API syntax:** `#shared` alias + مجلد `shared/` هو فيتشر Nuxt 4 حقيقي، والخطة صح إنه مش auto-import والاستيراد من `nuxt.config.ts` لازم يكون نسبي. `BaseTextarea` في my-base-layer فعليًا بيمرّر slot `#hint` لـ `UFormField` (اتأكّد من قراءة `F:\Code\personal\Temp\my-base-layer\app\components\base\Textarea.vue`). `maxlength` كـ attribute بيتمرّر فعليًا عبر `v-bind="innerAttrs"`.
- **Security:** حذف `serviceRoutes.js` مستقل تمامًا (auth middleware inline، مفيش أي ملف تاني بيعتمد عليه). `resumeUrl` أصلاً عام (بيرجع من `/landing` من غير auth) فمفيش تغيير في مستوى الحساسية.

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

(فاضي — الجولة دي ما لقتش أي بند 🟡 يستاهل تسجيل.)

### قرارات محسومة اتضافت للخطة

- **تقصير مقال «Hoisting» (206→≤160 حرف)** ضروري بنفس منطق «ورّاق» — أُضيف لخطة الـ db (القرارات المحسومة + Milestone 3 + معايير القبول). السبب: الفحص التنفيذي الأصلي غطّى المشاريع بس. (المصدر: فحص تنفيذي فعلي — جولة مراجعة 1)
- **أرقام أطوال «ورّاق»/«ترابط»** اتحدّثت في خطة السيرفر من (467/194) لـ (322/147) — عكس حالة المحتوى الفعلية وقت المراجعة.

## جولة 2 — 2026-07-11

**اللي اتغيّر من الجولة اللي فاتت (مدخل المراجعة التفاضلية):** إصلاحات جولة 1 — إضافة مهمة تقصير مقال «Hoisting» (206→≤160) في خطة الـ db (Milestone 3 + معايير القبول + Edge cases + Dependencies)؛ تحديث خطة السيرفر (Edge cases + Milestone 1 + القرارات المحسومة) بنتيجة فحص المقالات؛ تحديث أرقام «ورّاق»/«ترابط» (467/194 → 322/147).

**ملاحظة عن العدسات:** الأربع عدسات (data model، cross-project contract، library/API syntax، security) اتشغّلت inline على الكود الفعلي (مش subagents — تفادياً لتكلفة الـ spawn ولحدّ الجلسة اللي أفشل subagents جولة 1، وبنفس منهجية جولة 1) + فحص تنفيذي فعلي على API الإنتاج (`api.beingmomen.com` — `/projects` و`/blogs` عبر `getAll` عام بدون auth، مقيسين بالـ code-point length في Node).

### النتايج والقرارات

- [x] 🟠 تغطية ناقصة لإعادة تسمية شارة التوفّر — الخطة بتغيّر النص وبتطلبه في معيار القبول، بس «المناطق المتأثرة» بتدرج `Hero.vue` بس. النص `'متاح للمشاريع الجديدة'` متكرّر حرفياً في **٣ ملفات**: `Hero.vue:43`، `about.vue:134`، `contact.vue:106`. تطبيق حرفي → الهوم يتحدّث بينما `/about` و`/contact` يفضلوا بالنص القديم، اللي (أ) بيخالف معيار القبول، (ب) بيهدم هدف «التموضع المحايد» على صفحتين، (ج) **مايتمسكش** من grep الأمان بتاع Milestone 2 (العبارة مش ضمن قائمته). — **قرار:** اتصلح (موافقة المستخدم): أُضيف `about.vue` و`contact.vue` لـ«المناطق المتأثرة» + Milestone 3 لإعادة التسمية في التلاتة؛ أُضيفت «متاح للمشاريع الجديدة» لقائمتَي grep (Milestone 2 + فحص السلامة في Edge cases)؛ حُدِّث معيار القبول لـ«التلاتة»؛ أُضيف قرار محسوم للخطة. التوصية **مدّت** قرار «تمريرة صوت على شارة التوفّر» القايم — مش عكسته. — **دليل التحقق:** `grep "متاح للمشاريع الجديدة" apps/client` (باستثناء Plan/) → ٣ نتايج (Hero:43, about:134, contact:106)؛ الخطة قبل الإصلاح تدرج ملف واحد.

### ملاحظة إضافية (دليل تنفيذي مصاحب — عمّق الفحص الجزئي لجولة 1)

قِست **كل** المشاريع والمقالات (مش عيّنة): `GET /api/v1/projects?fields=title,description,isActive&limit=100` → 4 مشاريع: ورّاق **322** (>200 ⛔)، ترابط **147** (✅)، عصام فهمي **134** (✅)، دريم تي في بلاير **120** (✅). `GET /api/v1/blogs?fields=title,description,status&limit=100` → **مقال واحد إجمالاً** (published): Hoisting **206** (>160 ⛔). الاستنتاج: قائمة «التقصير قبل التفعيل» (ورّاق + Hoisting) **كاملة ومؤكَّدة**؛ المشروعان اللي فاتوا جولة 1 (عصام/دريم) الاتنين داخل الحدود؛ مفيش drafts مخبية. `getAll` للاتنين بدون فلتر (`status`/`isActive`) فالقياس شامل كل الحالات.

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **Data model & lifecycle:** الحد الأدنى الجديد `min:60` — كل الأوصاف الموجودة ≥120 حرف (أدناها دريم=120)، فمفيش أي داتا قائمة (مشاريع أو مقال) هيكسرها التفعيل. `Project.description`/`Blog.description` الاتنين `required` أصلاً (مفيش undefined). المسودّات المقترحة كلها داخل [60,200] (اتأكّد جولة 1: 188/140/117/111).
- **Library/API syntax:** ادعاء الخطة عن `errorController` مؤكَّد صحيح — `sendErrorProd` بيخصّص `CastError` (400) بس، و`ValidationError` (اللي عنده `err.errors`) بيقع على `else` → 500 «Something went wrong!» في الإنتاج؛ يعني طبقة express-validator (400) مبرَّرة. `ProjectsForm.vue:65` و`BlogForm.vue:58` بيستخدموا `BaseTextarea` فعلاً لحقل الوصف → خطة عدّاد `#hint` + `:maxlength` سليمة.
- **Cross-project contract:** إزالة مفتاحَي `services`/`skills` من `/landing` آمنة — grep على `apps/client` مالقاش أي مستهلك لـ`landing.services` ولا top-level `landing.skills`؛ `about.vue` بياخد `info.skills` (المضمّن) من `/infos` منفصل (سطر 5+26)، و`landing.info.resumeUrl` متضمّن في `.select()` بتاع `landingController` (لزرار CV). `Projects.vue` بقى بياخد من `/home-featured/populated` (key منفصل `landing-projects`) — مالوش علاقة بالحذف.
- **Security:** مفيش تغيير في مستوى الحساسية — `getAll` للمشاريع/المقالات عام أصلاً (بدون `protect`)، والحذف كله في `landingController`/موديول Service المستقل.

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

- نص شارة التوفّر متكرّر hardcoded في ٣ ملفات — مرشّح للمركزة في مصدر واحد (`shared/identity.ts` أو `app.config`) لمنع الـ drift مستقبلاً (الإصلاح الحالي وحّد النص لكن سابه متكرّر).
- خطة الـ db بتكتب `description: z.string().min(60,'…').max(…)` بشكل مختصر — المنفّذ لازم يحافظ على `z.string({ message: '…' })` في الـ constructor حسب اتفاقية `apps/db/CLAUDE.md` (رسالة الحقل المفقود بالعربي)، مش يستبدلها بـ`z.string()` مجرّدة.
- `docs/STATIC_DATA_REPORT.md` لسه فيه لقطات قديمة «5 سنوات»/«+50 عميل» (أسطر 63/74/173/175)؛ Milestone 5 بيحدّثه لتعليم Stats بس، من غير ما ينصّ على تنظيف اللقطات دي (مش نص معروض — توثيق بس).

### قرارات محسومة اتضافت للخطة

- **إعادة تسمية شارة التوفّر عبر التلاتة** (Hero + about + contact) — أُضيفت لخطة الـ client (القرارات المحسومة + Milestone 3 + المناطق المتأثرة + معايير القبول)، والعبارة القديمة أُضيفت لقائمتَي grep الأمانية. (المصدر: جولة مراجعة 2 — grep تنفيذي)

**اللي اتغيّر في الجولة دي (مدخل الجولة الجاية للمراجعة التفاضلية):** خطة الـ client فقط — المناطق المتأثرة (about/Hero/contact) + Edge cases grep + معيار قبول الشارة + Milestone 2 grep + Milestone 3 + القرارات المحسومة. خطتا السيرفر والـ db **ما اتغيّروش** هذه الجولة.

## جولة 3 — 2026-07-11

**اللي اتغيّر من الجولة اللي فاتت (مدخل المراجعة التفاضلية):** إصلاحات جولة 2 على خطة الـ client فقط — توسعة تغطية شارة التوفّر لـ about/contact (المناطق المتأثرة + Milestone 3 + معيار القبول)، وإضافة «متاح للمشاريع الجديدة» لشبكتَي grep (Milestone 2 + Edge cases). خطتا السيرفر والـ db ثابتتين من جولة 2 (موثّق تحقّقهما هناك — مفيش إعادة فحص).

**ملاحظة عن العدسات:** المراجعة التفاضلية اترّكزت على المنطقة اللي جولة 2 عدّلتها (تغطية شارة التوفّر) + هنت العدسات inline على الكود الفعلي (نفس منهجية جولتَي 1 و2، بدون subagents لحدّ الجلسة) + فحص تنفيذي بـ grep على كود الـ client الحقيقي. تأكّدت إن قائمة «المناطق المتأثرة» بتغطّي **كل** مواضع العبارات الرقمية الممنوعة (`app.config.ts:6,40`، `index.vue:20,29,31,37`، `nuxt.config.ts:25,35,40,67`، `about.vue:21,34`، `public/llms.txt:3`، وحذف `Stats.vue:10,32` بالكامل) — القائمة شاملة، مفيش موضع رقمي فايت.

### النتايج والقرارات

- [x] 🟠 تغطية شارة التوفّر لسه ناقصة موضع رابع معروض — قرار جولة 2 نصّ إن النص متكرّر في «٣ ملفات» وعالج التلاتة (Hero:43, about:134, contact:106)، بس فحص تنفيذي (`grep "متاح لل"`) كشف مؤشّر توفّر **رابع** بصيغة مختلفة `'متاح للمشاريع'` في `contact.vue:392` (كارت التواصل، منفصل عن بادج الهيرو سطر 106). تطبيق الخطة حرفياً → صفحة `/contact` الواحدة تعرض صيغتين مختلفتين (هيرو «متاح للفرص والمشاريع» ✅ + كارت «متاح للمشاريع» ❌ اللي بيستثني «الفرص/الوظيفة»)، وده (أ) يخالف قرار التموضع المحايد المحسوم على نفس الصفحة، (ب) تناقض بصري على صفحة واحدة، (ج) **يهرب من شبكة grep** لأن الـ literal مختلف («متاح للمشاريع» مش «متاح للمشاريع الجديدة») ومعيار القبول القديم مكتوب على الـ literal الطويل. — **قرار:** اتصلح (موافقة المستخدم): أُضيف `contact.vue:392` لـ«المناطق المتأثرة» + Milestone 3 + معيار القبول؛ اتوسّعت شبكة grep الأمانية (Edge cases + Milestone 2) للنمط الأعمّ `متاح للمشاريع` اللي بيمسك الصيغتين؛ حُدِّث القرار المحسوم من «التلاتة» لـ«الأربع مواضع». التوصية **مدّت** قرار جولة 2 (تمريرة صوت على شارة التوفّر) — مش عكسته. — **دليل التحقق:** `grep -n "متاح لل" apps/client/app` → 4 نتايج معروضة (Hero:43, about:134, contact:106, **contact:392**)؛ الخطة قبل الإصلاح تدرج 3 وشبكة grep بتستخدم الـ literal الطويل اللي بيفوّت 392.

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **تغطية الأرقام الممنوعة (نفس نمط ثغرة الشارة):** grep على كل العبارات الممنوعة في `apps/client/app` + `nuxt.config.ts` + `public/llms.txt` → كل موضع معروض مغطّى فعلاً في «المناطق المتأثرة» بالخطة. مفيش موضع رقمي فايت (على عكس نص التوفّر). `twitterDescription/twitterTitle` (index.vue:37) فيهم «استشارة مجانية» بس Milestone 1 بيعيد اشتقاق كل `useSeoMeta` من `IDENTITY` (إعادة كتابة شاملة للبلوك) وشبكة grep بتاعت «استشارة مجانية» بتمسكه — مغطّى.
- **Cross-project / library / security:** ما اتفحصوش من جديد — التغيير التفاضلي كله داخل خطة الـ client في منطقة نصّية بحتة (شارة التوفّر)؛ ادعاءات السيرفر/الـ db موثّق تحقّقها في جولتَي 1 و2 وسياقها ما اتغيّرش (قاعدة عدم إعادة التحقق).

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

(فاضي — الجولة دي ما لقتش بند 🟡 جديد. بنود جولة 2 التلاتة لسه قايمة للـ improve mode.)

### قرارات محسومة اتضافت للخطة

- **توحيد كل مواضع التوفّر الأربعة** (Hero:43 + about:134 + contact:106 + contact:392) لصيغة محايدة واحدة، وشبكة grep بالنمط الأعمّ `متاح للمشاريع` — امتداد لقرار جولة 2 (اللي غطّى ٣ مواضع بس). (المصدر: جولة مراجعة 3 — grep تنفيذي)

**اللي اتغيّر في الجولة دي (مدخل الجولة الجاية للمراجعة التفاضلية):** خطة الـ client فقط — «المناطق المتأثرة» (note الشارة + سطر contact) + Edge cases grep (نمط أعمّ) + معيار قبول الشارة + Milestone 2 grep + Milestone 3 + القرارات المحسومة. خطتا السيرفر والـ db **ما اتغيّروش**.

## جولة 4 — 2026-07-11

**اللي اتغيّر من الجولة اللي فاتت (مدخل المراجعة التفاضلية):** إصلاحات جولة 3 على خطة الـ client فقط — إضافة `contact.vue:392` كموضع توفّر رابع، وتوسيع نمط grep من الـ literal الطويل «متاح للمشاريع الجديدة» للنمط «متاح للمشاريع» (Edge cases + Milestone 2 + معيار القبول + Milestone 3 + القرارات المحسومة). خطتا السيرفر والـ db ثابتتين من جولة 2 (موثّق تحقّقهما — مفيش إعادة فحص).

**ملاحظة عن العدسات:** المراجعة التفاضلية اترّكزت على المنطقة اللي جولة 3 عدّلتها (شبكة grep الأمانية لمواضع التوفّر) + هنت العدسات inline على الكود الحقيقي (نفس منهجية جولات 1–3، بدون subagents لحدّ الجلسة) + فحص تنفيذي حقيقي: شغّلت `grep متاح` (النمط الأعمّ) و`grep -E "متوفر|available"` على `apps/client/app`، وشغّلت **نمط الخطة نفسه** `grep "متاح للمشاريع"` للتأكّد من تغطيته الفعلية.

### النتايج والقرارات

- [x] 🟠 شبكة grep الأمانية بتفوّت موضع توفّر خامس معروض في الفوتر — الخطة (بعد جولة 3) بتعدّ **٤ مواضع** وبتعتمد على `grep "متاح للمشاريع"` كدليل اكتمال (Edge cases + معيار القبول + Milestone 2)، بس فيه موضع **خامس** معروض بصياغة تخصّ المشاريع في `AppFooter.vue:26` (نص الـ CTA بانر: «أنا **متاح لمشاريع جديدة** ومستعد لتحويل أفكارك…») بيظهر **على كل صفحة** (منصوص عليه في `apps/client/CLAUDE.md`). بيهرب من الشبكة لأنه بلام **واحدة** «متاح لمشاريع» بينما النمط بلامين «متاح للمشاريع» → «متاح لمشاريع» مش substring من «متاح للمشاريع». سيناريو الفشل: المنفّذ يطبّق الأربعة + يشغّل grep الأمان → صفر نتايج (أخضر كاذب) → الفوتر يفضل بصياغة تخصّ المشاريع على كل صفحة، مخالفة لقرار «التموضع المحايد» المحسوم ومعيار القبول عدّى. **نفس صنف الثغرة** اللي أنتجت جولة 2 (literal «الجديدة» فوّت 392) وجولة 3 («متاح للمشاريع» فوّت الفوتر). — **قرار:** اتصلح (موافقة المستخدم): (١) أُضيف `AppFooter.vue:26` كموضع خامس صريح في «المناطق المتأثرة» + القرار المحسوم (٤→٥) + معيار القبول + Milestone 3؛ (٢) اتوسّع نمط grep الأماني من «متاح للمشاريع» لـ `grep -E "متاح لل?مشاريع"` (لام تانية اختيارية) في التلات مواضع (Edge cases + معيار القبول + Milestone 2). التوصية **مدّت** قرار جولتَي 2 و3 (توحيد مواضع التوفّر) — مش عكسته؛ نفس نمط التوسعة اللي عملوه. — **دليل التحقق:** `grep "متاح للمشاريع" apps/client/app` (نمط الخطة قبل الإصلاح) → **٤ نتايج بالظبط** (Hero:43, about:134, contact:106, contact:392) والفوتر:26 **مش فيهم** → الـ grep بيدّي أخضر كاذب. النمط الجديد `متاح لل?مشاريع` بيمسك التلات صيغ («للمشاريع الجديدة» + «للمشاريع» + «لمشاريع جديدة») من غير ما يمسك المحايدة «متاح للفرص والمشاريع» (بعد «متاح لـ» بييجي «فرص» مش «مشاريع») → معيار «صفر نتايج» يفضل قابل للتحقيق.

### ملاحظة إضافية (دليل تنفيذي مصاحب — جرد كامل لنصوص التوفّر)

`grep متاح` على `apps/client/app` رجّع كل نصوص التوفّر المعروضة: Hero:43، about:134، contact:106، contact:392 (الأربعة اللي في الخطة) + **AppFooter:26** (الخامس، الثغرة) + `contact.vue:78` «مصر — متاح عن بُعد». الأخير **مش finding** — «متاح عن بُعد» صياغة محايدة (بتخص مكان العمل عن بُعد، مش المشاريع-مقابل-الوظيفة)، ومتّسقة مع قرار التموضع المحايد صراحةً؛ ومابيتمسكش بالنمط الجديد. `grep -E "متوفر|available"` مالاقيش أي نص توفّر بديل. فالجرد دلوقتي **كامل ومؤكَّد: ٥ مواضع تخص المشاريع** كلها مغطّاة.

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **Data model / cross-project / library / security:** ما اتفحصوش من جديد — التغيير التفاضلي كله داخل خطة الـ client في منطقة نصّية بحتة (مواضع التوفّر، مدفوعة بـ`global.available` boolean بدون أي schema/contract/component جديد)؛ ادعاءات السيرفر/الـ db موثّق تحقّقها في جولات 1 و2 وسياقها ما اتغيّرش (قاعدة عدم إعادة التحقق).

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

(فاضي — الجولة دي ما لقتش بند 🟡 جديد. بند جولة 2 «مركزة نص الشارة في مصدر واحد `shared/identity.ts`/`app.config`» بقى أقوى دلوقتي — ٥ مواضع متكرّرة بدل ٣ — لكنه لسه للـ improve mode. بنود جولة 2 التانية لسه قايمة.)

### قرارات محسومة اتضافت للخطة

- **موضع التوفّر الخامس (`AppFooter:26`) + نمط grep بلام اختيارية** (`متاح لل?مشاريع`) — امتداد لقرارَي جولتَي 2 و3؛ العدّ بقى ٥ مواضع، وشبكة الأمان بتمسك صيغة الفوتر ذات اللام الواحدة. (المصدر: جولة مراجعة 4 — grep تنفيذي بنمط الخطة نفسه)

**اللي اتغيّر في الجولة دي (مدخل الجولة الجاية للمراجعة التفاضلية):** خطة الـ client فقط — «المناطق المتأثرة» (note الشارة ٤→٥ + سطر الفوتر/contact) + Edge cases grep (نمط لام اختيارية) + معيار قبول الشارة (٥ مواضع) + Milestone 2 grep + Milestone 3 (الخامس) + القرار المحسوم (٤→٥). خطتا السيرفر والـ db **ما اتغيّروش**.

## جولة 5 — 2026-07-11

**اللي اتغيّر من الجولة اللي فاتت (مدخل المراجعة التفاضلية):** إصلاحات جولة 4 على خطة الـ client فقط — إضافة `AppFooter:26` كموضع توفّر خامس، وتغيير نمط grep الأماني من الـ literal «متاح للمشاريع» لـ`متاح لل?مشاريع` (لام تانية اختيارية) في التلات مواضع + القرار المحسوم. خطتا السيرفر والـ db ثابتتين من جولة 2 (مفيش إعادة فحص).

**ملاحظة عن العدسات:** المراجعة التفاضلية اترّكزت على نص جولة 4 الجديد غير المُراجَع = **نمط grep الجديد نفسه**. بدل ما أكتفي بالاستنتاج المنطقي (اللي جولة 4 اكتفت بيه)، **نفّذت النمط فعلياً** (rule 4) في الـscratchpad على القيم الحقيقية الست (التلات صيغ القديمة + المحايدة الجديدة + «متاح عن بُعد» + مسودّة الفوتر) عبر `grep -E` (byte-oriented، الأداة اللي الخطة بتنص عليها) **و**ripgrep (unicode-aware) للمقارنة، + hexdump للبايتات لتفسير السبب الجذري.

### النتايج والقرارات

- [x] 🟠 نمط grep بتاع إصلاح جولة 4 (`متاح لل?مشاريع`) مكسور في `grep -E` وبيفوّت الفوتر اللي اتعمل عشانه — جولة 4 اختارت `لل?` بافتراض منطقي («لام تانية اختيارية = يمسك لام أو لامين») **من غير تنفيذ فعلي**. التنفيذ في جولة 5 كشف إن الـ`grep -E` الـbyte-oriented (LANG فاضية، مفيش UTF-8 regex كامل) بيطبّق الـ`?` على **آخر بايت من اللام** (`0x84`) مش على الحرف، فـ`لل?` = `d9 84 d9 (84)?` = **لامين إجبارياً + التاني اختياري** → بيمسك «للمشاريع» بس ويفوّت «لمشاريع» الفوتر. سيناريو الفشل: المنفّذ ينسى توحيد الفوتر → الفوتر يفضل «أنا متاح لمشاريع جديدة» على كل صفحة → `grep -E "متاح لل?مشاريع"` يرجّع صفر (أخضر كاذب) → معيار القبول عدّى والعيب حيّ = **نفس الثغرة اللي جولة 4 اتعملت عشان تقفلها، متعادة بالإصلاح نفسه**. — **قرار:** اتصلح (موافقة المستخدم على التوصية أ): استبدال النمط في التلات مواضع (Edge cases + معيار القبول + Milestone 2) + القرار المحسوم بـalternation للـ literalين الكاملين `grep -E "متاح لمشاريع|متاح للمشاريع"` — byte-safe ومستقل عن الأداة/الـlocale (مفيش quantifier على حرف عربي). **ده تصحيح صريح لاختيار جولة 4** (`لل?`) — مش أوسكلة: مدعوم بدليل تنفيذي جديد جولة 4 ما شغّلتوش (تجسيد حي لمبدأ rule 4: التنفيذ بيمسك اللي القراءة المنطقية بتفوّته). — **دليل التحقق:** `grep -nE "متاح لل?مشاريع" avail-test.txt` → سطر 1+2 بس (الفوتر سطر 3 مش متمسك ❌)؛ `grep -nE "متاح لمشاريع|متاح للمشاريع"` → 1+2+3 ✅ ومفيش 4 (المحايدة) ولا 5 («عن بُعد») ولا 6 (مسودّة الفوتر)؛ نفس النمط `لل?` في ripgrep بيمسك 1+2+3 (فالكسر خاص بـ`grep -E`). hexdump أكّد «للمشاريع»=`d9 84 d9 84 d9 85` و«لمشاريع»=`d9 84 d9 85`.

### ملاحظة إضافية (اكتمال التعداد)

الجزء التاني من إصلاح جولة 4 (التعداد ٤→٥ وإضافة `AppFooter:26`) **صحيح ومتحقَّق** — جرد `grep متاح` (جولة 4) رجّع نفس الـ٥ مواضع تخص المشاريع + `contact:78` المحايد؛ الكود ما اتغيّرش، فالتعداد ثابت. العيب الوحيد كان في **آلية الكشف** (النمط)، مش في التعداد.

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **Data model / cross-project / library / security:** ما اتفحصوش من جديد — التغيير التفاضلي كله داخل خطة الـ client في سطر أمان نصّي (نمط grep)، بدون أي schema/contract/component؛ ادعاءات السيرفر/الـ db موثّق تحقّقها في جولات 1–2 وسياقها ما اتغيّرش (قاعدة عدم إعادة التحقق).

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

(فاضي — مفيش بند 🟡 جديد. بند «مركزة نص التوفّر في مصدر واحد» — لو النص كان مصدر واحد مكانش احتجنا نمط grep يمسك ٣ صيغ من الأساس — بقى أقوى للـ improve mode، لكنه لسه مؤجّل. بنود جولة 2 التانية قايمة.)

### قرارات محسومة اتضافت للخطة

- **نمط grep الأماني = alternation `متاح لمشاريع|متاح للمشاريع`** (مش quantifier `لل?`) — لأن الـ`?` بيتكسر على الحروف العربية متعددة البايت في byte-oriented `grep -E`. تصحيح لاختيار جولة 4. (المصدر: جولة مراجعة 5 — تنفيذ scratchpad + hexdump)

**اللي اتغيّر في الجولة دي (مدخل الجولة الجاية للمراجعة التفاضلية):** خطة الـ client فقط — نمط grep الأماني اتغيّر من `متاح لل?مشاريع` لـ`متاح لمشاريع|متاح للمشاريع` في 4 مواضع (Edge cases + معيار القبول + Milestone 2 + القرار المحسوم)، مع تحديث النص التوضيحي لسبب الـalternation. **مفيش تغيير في التعداد (٥ مواضع) ولا في أي منطق تاني.** خطتا السيرفر والـ db **ما اتغيّروش**.

## تعديل بنيوي بعد جولة 5 — 2026-07-11 (تبنّي بند 🟡 — مش جولة مراجعة)

**السياق:** بعد جولة 5 عرضت مخرجين: (1) جولة مراجعة سادسة، أو (2) الحل البنيوي (مركزة نص التوفّر) اللي بيوقف الـ thread من جذره. المستخدم اختار (2)، وبعدها وافق على التصميم الكامل (الخيار أ). ده تبنّي مبكّر لبند 🟡 من الـ backlog — **بيتعمل هنا كتعديل خطة، مش جولة مراجعة** (مفيش hunt جديد؛ الجولة الجاية هي اللي بتراجعه).

**السبب الجذري اللي بيحلّه:** الـ grep whack-a-mole عبر جولات 2→5 (كل جولة صيغة تهرب) عرض لمرض واحد = **التكرار**: نص التوفّر كان hardcoded ٨ مرات (٤ true: ٣× «متاح للمشاريع الجديدة» + ١× «متاح للمشاريع» في contact:392؛ + ٤ false: «غير متاح حالياً»). المصدر الواحد بينهي الفئة كلها.

**اللي اتغيّر في الخطة (١٠ تعديلات على خطة الـ client):**
- **المناطق المتأثرة:** `shared/identity.ts` يضيف `availableLabel='متاح للفرص والمشاريع'` + `unavailableLabel='غير متاح حالياً'`؛ `app.config.ts` يشتق `global.availableLabel/unavailableLabel` (نفس آلية `global.available`)؛ ملاحظة الهيرو اتعاد تأطيرها للمركزة؛ سطر البادجات بيرجع للمصدر.
- **Edge cases + معيار القبول:** شبكة grep الدائمة → **regression check لمرة واحدة** (`grep -E "متاح للمشاريع|متاح لمشاريع"` = صفر — byte-safe، لصيغتَي الـ literal القديمة).
- **القرار المحسوم:** اتحدّث لـ«نص التوفّر مُمركَز»، وبيَنسخ صراحةً نهج «شبكة grep دائمة» المحسوم في جولات 2→5.
- **Milestone 1:** يضيف الثابتين + wiring؛ **Milestone 3:** الـ٥ مواضع تبقى references (مش rewrite فردي)؛ **Milestone 2:** شيلت منه regression التوفّر (بيحصل في M3، مش M2 — تصحيح ترتيب كامن).
- **مسودّات:** قسم «شارة التوفّر» بقى يوثّق قيمتَي الثابتين.

**تنبيه أمين — ده نص خطة جديد غير مُراجَع:** التبنّي بيعمل **reset للمراجعة التفاضلية**. القرار نفسه محسوم (اختيار المستخدم)، لكن **ميكانيكا تطبيقه في الخطة لسه محتاجة مراجعة** في جولة 6. عدّاد الجولات النظيفة **ما اتغيّرش** (لسه صفر). مايتقالش «جاهزة للتنفيذ».

**نقاط لازم جولة 6 تتحقق منها (head start):**
1. `app.config` يقدر يحمل `availableLabel/unavailableLabel` وتكون متاحة في SSR — **مبدئياً منخفض الخطورة** لأنها نفس آلية `global.available` القائمة (اتأكّد إن `available: true` فعلاً تحت `global` في app.config).
2. الأربع بادجات **مش متطابقة حرفياً قبل التوحيد**: ٣ بترنري `:label` بيقول «متاح للمشاريع الجديدة»، و`contact:392` interpolation `{{ }}` بيقول «متاح للمشاريع» (من غير «الجديدة»). المركزة بتوحّدهم للثابت — اتأكّد إن الشكلين (`:label` و`{{ }}`) الاتنين بيتعاملوا صح.
3. الفوتر (AppFooter:26) **مش بادج ومش ternary** — جملة مستقلة، بتفضل جملة الفوتر CTA المحايدة (مش بترجع `availableLabel`). اتأكّد إن ده متّسق مع مسودّة الفوتر CTA.
4. الثابت «متاح للفرص والمشاريع» **مابيطابقش** الـ regression grep `متاح للمشاريع|متاح لمشاريع` (اتأكّد نظرياً: بعد «متاح ل» بييجي «فرص» — فصفر false positives)؛ + FAQ «متاح لفرص»/«متاح لوظيفة» وcontact:78 «متاح عن بُعد» كلها برّه النمط.

**تحديث الـ 🟡 backlog:** بند «مركزة نص التوفّر في مصدر واحد» (اتسجّل جولة 2، اتقوّى جولة 5) بقى **مُتبنّى** — اتشال من الـ backlog وبقى في الخطة. بنود جولة 2 التانية (رسالة Zod العربية في db + لقطات `STATIC_DATA_REPORT.md` القديمة) لسه قايمة للـ improve mode.

**اللي اتغيّر (مدخل جولة 6 للمراجعة):** خطة الـ client — مقاربة نص التوفّر اتحوّلت من «٥ literals hardcoded + شبكة grep» لـ«مصدر واحد `global.availableLabel/unavailableLabel` + ٥ references + regression لمرة واحدة». اتأثّرت: المناطق المتأثرة (identity/app.config/ملاحظة الهيرو/سطر البادجات) + Edge cases + معيار القبول + القرار المحسوم + Milestones 1/2/3 + مسودّات. خطتا السيرفر والـ db **ما اتغيّروش**.

## جولة 6 — 2026-07-11

**اللي اتغيّر من الجولة اللي فاتت (مدخل المراجعة التفاضلية):** التعديل البنيوي بعد جولة 5 — مركزة نص التوفّر (تفاصيله في القسم اللي فوق). دي مراجعة **حقيقية** للميكانيكا الجديدة، مش differential تافه.

**ملاحظة عن العدسات:** هنت inline على الكود الحقيقي (نفس منهجية الجولات السابقة) بتدقيق **أعلى** لأن التعديل مكتوب في نفس الجلسة (خطر rubber-stamp للشغل الذاتي) + فحص تنفيذي: قراءة `app/app.config.ts` الفعلي للتأكد من بنية `global`، و`grep -E` في الـscratchpad على قيم ما-بعد-الـrefactor.

### النتايج والقرارات

(مفيش 🔴 ولا 🟠 — جولة نظيفة.)

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **بنية `global` (data model الميكانيكا):** ✅ `app.config.ts:19` فيها `available: true` تحت مفتاح `global` فعلاً → namespace `global.availableLabel/unavailableLabel` صح ومتّسق. الأربع بادجات بتقرا `global.available` أصلاً (grep جولة 4)، فبتقدر تقرا `global.availableLabel` من نفس الـscope.
- **الشكلين للبادجات:** ✅ التلاتة `:label="global.available ? … : …"` + `contact:392` interpolation `{{ global.available ? … : … }}` — التعبير المركزي `global.available ? global.availableLabel : global.unavailableLabel` بيركب في الشكلين بدون مشكلة.
- **regression grep (تنفيذي):** ✅ `grep -E "متاح للمشاريع|متاح لمشاريع"` على قيم ما-بعد-الـrefactor (الثابت «متاح للفرص والمشاريع» + جملة الفوتر + سطر الـreference) → **exit 1، لا match**. فالنمط بيمسك الـliteralات القديمة بس، وبيدّي صفر بعد التطبيق الكامل. الثابت «متاح للفرص والمشاريع» مابيطابقش لأن بعد «متاح ل» بييجي «فرص».
- **مفيش موضع label سادس:** ✅ باقي استخدامات `global.available` (contact/about/Hero) كلها `:color`/`:class`/`:to`/`:target` — سلوك/تنسيق مش نص شارة. `contact:396` `{{ global.available ? 'الرد خلال 24 ساعة' : 'قائمة الانتظار مفتوحة' }}` نص وقت-رد مختلف (مش تموضع «متاح لـ») — خارج الـscope صح.
- **اتساق الخطة الداخلي:** ✅ مفيش تأطير «٥ hardcoded تتعاد كتابتها» متبقّي؛ القرار المحسوم (سطر 90) بيَنسخ نهج «شبكة grep دائمة» صراحةً. Milestone 1 (الثوابت) قبل Milestone 3 (الـreferences) — ترتيب سليم.
- **Cross-project / security:** N/A — التعديل client بحت، `available` config ثابت (مش من الـAPI)، ومفيش auth.
- **قاعدة عدم إعادة التحقق:** ميكانيكا استيراد app.config↔IDENTITY (نسبي/`#shared`) بلاش إعادة فتح — pre-existing من round 1، والمركزة بتضيف حقلين لنفس الـderive، مش بتغيّر آلية الاستيراد.

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

(مفيش بند جديد. بنود قايمة للـ improve mode: رسالة Zod العربية في خطة الـ db + لقطات `STATIC_DATA_REPORT.md` القديمة «5 سنوات»/«+50 عميل».)

### قرارات محسومة اتضافت للخطة

(مفيش — الجولة ما لقتش حاجة تتصلّح، والقرار البنيوي كان اتّضاف بالفعل مع التعديل اللي فوق.)

**اللي اتغيّر في الجولة دي (مدخل الجولة الجاية للمراجعة التفاضلية):** **لا شيء** — جولة نظيفة، مفيش أي تعديل على أي خطة. differential جولة 7 فاضي؛ لو طلعت نظيفة (متوقّع) → القفل 🔒 (جولتين نظاف: 6 و7).

## جولة 7 — 2026-07-11 (جولة القفل)

**اللي اتغيّر من الجولة اللي فاتت (مدخل المراجعة التفاضلية):** **لا شيء** — جولة 6 ما عدّلتش أي خطة، فالـ differential فاضي. الدور الوحيد لجولة 7: تأكيد إن الوضع ثابت + إعادة تحقق تنفيذي من الادعاءات الحاملة قبل القفل (مش rubber-stamp للجولة النظيفة الأولى).

**ملاحظة عن العدسات:** مع differential فاضي، ركّزت على **إعادة التحقق التنفيذي** من العمود الفقري للخطة كله (اللي دارت حوله جولات 2→6): مواضع التوفّر الخمسة + نمط الـ regression grep. قرأت الكود الحقيقي مباشرة (`grep متاح` على `apps/client/app` + قراءة `app.config.ts`) وشغّلت النمط في الـ scratchpad على قيم ما-قبل/ما-بعد الـ refactor.

### النتايج والقرارات

(مفيش 🔴 ولا 🟠 — جولة نظيفة.)

### عدسات اتفحصت من غير ما تلاقي حاجة نجت من التحقق

- **مواضع التوفّر الخمسة (تحقّق من أرقام السطور الحالية):** ✅ `grep متاح` على `apps/client/app` رجّع بالظبط الـ٥ مواضع اللي الخطة بتعدّها بنفس أرقام السطور: `Hero.vue:43` + `about.vue:134` + `contact.vue:106` («متاح للمشاريع الجديدة») + `contact.vue:392` («متاح للمشاريع» بلام واحدة) + `AppFooter.vue:26` («أنا متاح لمشاريع جديدة»). `contact.vue:78` («مصر — متاح عن بُعد») محايد وخارج النطاق صح؛ ومواضع `adr/multi-mode-system.vue` («الصفحات المتاحة»/«صفحة متاحة») سياق مختلف تماماً (أنظمة صفحات، مش تموضع شغل). الكود ما اتغيّرش من التخطيط، فأرقام السطور في الخطة لسه دقيقة.
- **بنية `global.available` (data model الميكانيكا):** ✅ `app.config.ts:19` فيها `available: true` تحت مفتاح `global` — namespace `global.availableLabel/unavailableLabel` سليم، والبادجات الأربعة بتقرا من نفس الـ scope أصلاً.
- **نمط الـ regression grep (تنفيذي، byte-safe):** ✅ شغّلت `grep -E "متاح للمشاريع|متاح لمشاريع"` في الـ scratchpad على القيم السبعة الحقيقية: بيمسك التلات صيغ القديمة بالظبط (لامين تمسك البادجات سطرَي 1+2، ولام واحدة تمسك الفوتر سطر 3) و**مابيمسكش** الثابت المحايد الجديد «متاح للفرص والمشاريع» (بعد «متاح ل» بييجي «فرص») ولا «غير متاح حالياً» ولا «متاح عن بُعد» ولا FAQ «متاح لفرص». فمعيار «صفر نتايج بعد التطبيق الكامل» قابل للتحقيق ودقيق.
- **خطتا السيرفر والـ db:** ✅ ثابتتان من جولة 2 (مفيش تعديل من وقتها — موثّق في اللوق)؛ ادعاءاتهما (حدود الطول 60–200/60–160، فصل Skill≠Service، طبقة express-validator ضد 500 المبهم، تقصير «ورّاق» 322 و«Hoisting» 206 قبل التفعيل) موثّق تحقّقها التنفيذي في جولتَي 1 و2، وسياقها ما اتغيّرش — قاعدة عدم إعادة التحقق تنطبق.

### 🟡 backlog (لوضع التحسين — متتناقشش هنا)

(مفيش بند جديد. بندان قايمان للـ `/my-improve`: [1] رسالة Zod العربية في خطة الـ db — الحفاظ على `z.string({ message: '…' })` في الـ constructor حسب `apps/db/CLAUDE.md` بدل `z.string()` المجرّدة؛ [2] لقطات `docs/STATIC_DATA_REPORT.md` القديمة «5 سنوات»/«+50 عميل» اللي Milestone 5 ماينصّش على تنظيفها صراحةً.)

### قرارات محسومة اتضافت للخطة

(مفيش — الجولة ما لقتش حاجة تتصلّح؛ كل القرارات محسومة ومُرقّاة للخطط بالفعل.)

**اللي اتغيّر في الجولة دي:** **لا شيء في الخطط** — جولة نظيفة تانية متتالية. تحديث اللوق فقط: صف الجدول 7 + قلب الحالة لـ🔒 مقفولة. **القفل نشط.**

---

## 🔒 الخطة مقفولة — 2026-07-11

جولتان نظيفتان متتاليتان (6 و7 — كلاهما 0🔴/0🟠). العمود الفقري للخطة (مركزة نص التوفّر + حدود الطول + حذف Service/stats/skills-في-landing) اتحقّق تنفيذياً عبر ٧ جولات.

**الخطوات المتاحة:**
- **`/my-improve`** (اختياري) — تريّاج بندَي الـ 🟡 backlog مرة واحدة (رسالة Zod العربية + لقطات STATIC_DATA_REPORT).
- **`/my-implement`** — بناء الخطة milestone-by-milestone (السيرفر أولاً: حدود الطول + الحذف؛ ثم الـ db: مطابقة الحذف + التقصير قبل التفعيل + الحواجز؛ ثم الـ client: المركزة + الصوت + زرار CV).

أي تعديل لاحق على أي من الخطط التلاتة بيكسر القفل ويستلزم differential review صريح على المتغيّر.
